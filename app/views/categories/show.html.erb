<% @feed = @feed.sort_by{ |i| i['score'] }.reverse %>
<% if @feed.count == 0 %>
  <div class="interior">
    <h3>This category is empty</h3>
  </div>
<% end %>
<% @feed.each do |x| %>
  <div class="post">
    <div class="image">
      <% if !x['image'].nil? %>
        <img src="<%= x['image'] %>">
      <% end %>
    </div>
    <div class="content">
      <p><%= link_to x['title'], x['url'] %></p>
      <p><%= x['author'] %></p>
      <% if x['votes'] != 1 %>
        <p><%= x['votes'] %> votes</p>
      <% else %>
        <p><%= x['votes'] %> vote</p>
      <% end %>
    </div>
    <div class="score">
      <p><%= x['score'] %></p>
    </div>
  </div>
<% end %>
